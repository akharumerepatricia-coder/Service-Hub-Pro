{
  "version": 2,
  "framework": null,
  "buildCommand": "pnpm --filter @workspace/canvica-crm run build && mkdir -p public && cp -r artifacts/canvica-crm/dist/public/. public/",
  "outputDirectory": "public",
  "installCommand": "pnpm install",
  "rewrites": [
    { "source": "/((?!api(?:/|$)).*)", "destination": "/index.html" }
  ],
  "env": {
    "ENABLE_EXPERIMENTAL_COREPACK": "1"
  }
}
